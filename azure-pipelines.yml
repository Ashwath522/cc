trigger:
  tags:
    include:
      - "deploy.*"

resources:
  repositories:
    - repository: Templates
      type: git
      name: Infrastructure/kube-infrastructure

extends:
  template: pipeline/azure-pipelines.yml@Templates #Replace with "integrations-azure-ci.yaml@Templates" for FIK integrations pipelines